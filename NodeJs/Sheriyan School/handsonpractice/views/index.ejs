<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Home</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
      <div class="form p-10 w-[80%] mx-auto flex flex-col justify-center">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 w-full rounded-lg outline-none bg-zinc-700"
            type="text"
            placeholder="Title"
            name="title"
          />
          <textarea
            class="block px-5 py-2 w-full rounded-lg outline-none my-2 resize-none bg-zinc-700"
            placeholder="Description"
            name="description"
          ></textarea>
          <div class="w-full flex justify-center items-center mt-4">
            <input
              class="bg-blue-600 py-2 px-12 rounded-lg cursor-pointer hover:bg-blue-500 transition-all duration-300"
              type="submit"
              value="Create Task"
              name="button"
            />
          </div>
        </form>
      </div>
      <div
        class="tasks w-[80%] mx-auto px-10 flex gap-3 items-center flex-wrap"
      >
        <% if (files.length > 0) { %> <% files.forEach((file) => { %>
        <div class="task w-72 h-auto px-3 py-4 bg-zinc-700 rounded-lg">
          <h1 class="font-bold text-xl"><%=file%></h1>
          <div class="w-full flex justify-between items-center">
            <a href="/file/<%= file%>" class="text-blue-400 inline-block mt-3"
              >Read more.</a
            >
            <a href="/edit/<%= file%>" class="text-yellow-400 inline-block mt-3"
              >Edit Name</a
            >
          </div>
        </div>
        <% }); %> <% } else { %>
        <h2 class="text-zinc-400">No Tasks yet ...</h2>
        <% } %>
      </div>
    </div>
  </body>
</html>
